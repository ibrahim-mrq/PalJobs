<resources xmlns:tools="http://schemas.android.com/tools">

    <!--  TODO : MaterialTheme  -->
    <style name="MaterialTheme" parent="Theme.MaterialComponents.Light.NoActionBar">
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:forceDarkAllowed" tools:targetApi="q">false</item>
        <item name="colorControlHighlight">@color/textSecondary</item>
        <item name="android:screenOrientation">portrait</item>
    </style>

    <!-- TODO :  corner ImageView   -->
    <style name="cornerImageView">
        <item name="cornerSize">50%</item>
    </style>

    <!-- TODO :  bottomBar   -->
    <style name="bottomBarStyle">
        <item name="android:textStyle">bold</item>
    </style>

    <!-- TODO :  navigation   -->
    <style name="navigationViewStyleBoldText">
        <item name="android:textColor">@color/black</item>
    </style>

    <!-- TODO :  Dialog Animation   -->
    <style name="animationName" parent="android:Animation">
        <item name="android:windowEnterAnimation">@anim/animation_on</item>
        <item name="android:windowExitAnimation">@anim/animation_off</item>
    </style>

    <!-- TODO :  image picker -->
    <color name="ucrop_color_toolbar">@color/colorPrimaryDark</color>
    <color name="ucrop_color_statusbar">@color/colorPrimaryDark</color>
    <color name="ucrop_color_widget_active">@color/colorPrimaryDark</color>

</resources>